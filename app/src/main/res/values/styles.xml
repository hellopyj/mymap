<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/main_c</item>
        <item name="colorPrimaryDark">@color/second_c</item>
        <item name="colorAccent">@color/third_c</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="MenuAnimationFade">
        <item name="android:windowEnterAnimation">@anim/popu_fadein</item>
        <item name="android:windowExitAnimation">@anim/popu_fadeout</item>
    </style>
</resources>
